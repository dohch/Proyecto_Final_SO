#ifndef IO_H
#define IO_H

// Funciones de I/O
void init_io();                    // Inicializa el sistema de I/O (si es necesario)
void write_data(const char *data);  // Simula la escritura de datos en la salida estándar
char* read_data();                 // Simula la lectura de datos de la entrada estándar

#endif
