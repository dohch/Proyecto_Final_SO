#ifndef UTILS_H
#define UTILS_H

// Funciones para manejo de errores
void handle_error(const char *message);    // Maneja errores imprimiendo un mensaje y terminando la ejecución

// Funciones auxiliares para depuración
void log_message(const char *message);     // Imprime mensajes de depuración

#endif
